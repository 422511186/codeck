# codeck 领域词汇

本词汇表定义 codeck 在会话内容呈现与本地资源访问中的统一语言。

## 会话内容

**本地 Markdown 图片引用**：
消息 Markdown 中以 macOS、Linux 或 Windows 绝对文件路径标识的图片资源；不包含相对路径、网络 URL 或内嵌数据。
_避免使用_：本地图片、文件图片

**Skill 引用**：
用户消息对某个 Codex Skill 的显式引用；结构化引用是标准形态，名称与绝对 `SKILL.md` 路径完整匹配的链接是兼容形态。
_避免使用_：Skill 链接、技能链接

## 模型配置

**模型接入配置**：
由 Codex 运行环境持有的一组模型路由与鉴权引用，包括 provider 标识、接口地址、协议类型和凭据来源；手机前端只能读取其脱敏状态并进行选择。
_避免使用_：模型提供商、模型渠道、Provider 配置

**自定义模型配置**：
由 Web 用户维护且不绑定特定模型接入配置的模型元数据，包括长度为 `1..256` 的模型标识、长度为 `1..100` 的显示名称、上下文窗口和能力声明；模型标识去除首尾空白并禁止控制字符。每条配置拥有不可变的 `customModelId`，执行时使用 Codex 当前激活的模型接入配置。它不包含接口地址或任何凭据值，并且不会因模型接入配置变化而消失。
_避免使用_：自定义模型、自定义模型条目、模型接入配置

**自定义模型目录**：
由 codeck 后端持久化的部署级全局自定义模型配置集合；连接同一实例的所有已认证设备共享读取和修改权限，目录最多包含 200 条配置，模型标识按区分大小写的精确值保持唯一。达到上限后只禁止创建，仍允许编辑和删除。设备本地只保存默认选择，不持有目录副本的所有权。
_避免使用_：自定义模型列表、本地模型列表、模型目录

**目录修订号**：
自定义模型目录每次成功变更后递增的整体版本标识；创建、完整替换和删除必须提交调用方最后读取的修订号，修订号不匹配时后端拒绝写入并返回最新目录，由调用方保留未提交内容并处理冲突。
_避免使用_：模型版本、配置版本、更新时间

**设备默认模型**：
浏览器本地保存的新会话结构化模型选择身份；自定义模型使用 `customModelId`，app-server 模型使用模型标识。创建会话时必须从后端重新解析对应模型定义，引用失效时清除并回退到 Codex 服务端默认模型，不在同名的不同来源之间自动改写。
_避免使用_：全局默认模型、后端默认模型、默认模型配置

**模型选择身份**：
模型选择在 Web 领域中的来源敏感身份，由自定义来源及 `customModelId`，或 app-server 来源及模型标识组成；模型标识相同但来源不同的两个选择身份不能互相替代。合并目录可隐藏同名 app-server 项，但不会消除其独立身份。
_避免使用_：模型 ID、模型字符串、合并模型身份

**模型能力声明**：
自定义模型配置对上下文窗口、输入模态和 reasoning 档位的显式描述；新建配置的上下文窗口默认为 `200000`，允许修改为 `1..1000000` 的安全整数，并在创建或重配到该自定义模型时作为请求级标称完整窗口覆盖传给 app-server。超过 `272000` 时，执行前要求该模型精确存在于 app-server 权威模型目录，避免未知模型 fallback 静默截断。app-server 目录模型不由 Web 覆盖上下文窗口，而由 app-server 自身模型定义及部署配置解析；turn 开始后，app-server token usage 返回的窗口是会话实际窗口。文本以外的输入模态及 reasoning 档位未声明即视为不支持。Reasoning 档位是去除首尾空白、禁止控制字符、区分大小写并保留精确值的开放字符串，最多声明 16 个互不重复的值且每个值长度为 `1..64`；声明档位时必须同时指定其中一个默认值，未声明 reasoning 时默认值为空。
_避免使用_：模型能力推断、默认模型能力、能力配置

**运行时实际窗口**：
app-server 通过 token usage 为当前会话运行时返回的 `modelContextWindow`；它与自定义模型配置中的标称窗口并存，不回写模型目录。两者不一致时，当前会话明确展示配置值与实际值，容量进度及后续安全判断使用实际值；provider 改变并重建运行时后，旧观察失效。
_避免使用_：模型上下文配置、有效配置窗口、目录实际窗口

**模型接入兼容性**：
自定义模型与 Codex 当前模型接入配置能否完成真实 turn 的运行时关系；除大于 `272000` 的标称窗口必须满足 app-server 权威模型目录前置条件外，系统不预先推断或发送测试请求，调用失败也不会删除自定义模型配置。
_避免使用_：模型可用性、Provider 可用性、模型验证状态

**输入兼容性**：
待发送草稿的输入模态与当前模型能力声明是否匹配；不匹配时保留草稿及附件并阻止发送，但不以历史消息中的输入模态阻断模型切换。
_避免使用_：附件校验、图片模型、历史兼容性

**可选模型目录**：
由 codeck 后端生成并供模型选择器使用的合并视图，按区分大小写的模型标识合并 app-server 模型与自定义模型配置；同名时自定义定义优先，但不修改 app-server 的原始模型目录。
_避免使用_：模型列表、合并模型列表、可用模型

**当前会话临时模型项**：
当前会话的模型选择身份未被可选模型目录表示时，由会话真实来源与模型标识生成的只读选择器项；包括模型已不属于任何目录，或无绑定的 app-server 会话被同名自定义项遮蔽的情形。它不是目录成员，不能编辑或设为设备默认模型，并在会话切换到其他模型后消失。
_避免使用_：未知模型、自定义模型、失效模型

**会话模型绑定**：
会话选择自定义模型时，由 codeck 后端按 `threadId` 持久化的配置快照，包括 `customModelId`、模型标识、上下文窗口、能力声明、来源配置更新时间、会话实际采用的 reasoning 档位和每次成功变更后重新生成的 `bindingVersion`。它不包含模型接入配置；每次重建运行时仍使用 Codex 当前模型接入配置。目录配置的编辑或删除不会改写已有绑定，只有用户显式重新应用或切换模型才会替换或移除绑定；会话 reasoning 档位更新成功时同步更新绑定。
_避免使用_：当前模型配置、模型实时引用、会话 Provider

**绑定操作记录**：
会话模型绑定变更前持久化的操作意图，包含操作标识、旧绑定和目标绑定；它使进程在运行时重建与绑定提交之间退出后，可以在下次访问会话时继续应用目标或恢复旧绑定。操作完成前，该会话不能发送 turn。
_避免使用_：切换缓存、临时绑定、切换状态

**模型切换**：
仅在会话空闲时，由 codeck 后端在保留 thread ID 与完整会话历史的前提下，使用目标模型及 Codex 当前模型接入配置重建会话运行时，并将切换或恢复结果作为一个确定命令返回；所有模型来源遵循同一切换语义。命令必须校验调用方最后读取的目录修订号、当前模型选择身份、reasoning 档位和绑定版本，任一过期时拒绝执行且不自动重试。
_避免使用_：更新模型设置、切换 Provider、重建会话

**切换容量预检**：
模型切换前将最近一次已知上下文用量与目标窗口比较的安全判断；达到目标窗口百分之九十时要求用户先手动压缩，用量未知时不阻止切换。
_避免使用_：上下文校验、自动压缩、Token 限制
