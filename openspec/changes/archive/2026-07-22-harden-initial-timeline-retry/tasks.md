## 1. 回归测试

- [x] 1.1 增加首屏分页失败后显示重试并在第二次成功后恢复页面的测试。
- [x] 1.2 增加重试再次失败仍保留最新错误和重试入口的测试。

## 2. 页面实现

- [x] 2.1 为首屏加载增加 attempt token，重试时清理错误并重新读取 metadata/page。
- [x] 2.2 在无 detail 错误视图增加 retry 按钮，并保持返回入口。

## 3. 验证

- [x] 3.1 运行页面定向测试、类型检查、完整 verify/build 与 OpenSpec 严格校验。
- [x] 3.2 独立复审重试期间的取消、迟到响应和空会话状态。
